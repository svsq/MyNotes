package com.svsq.mynotes

import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {

    /**
     * Looks too empty? No, because of integration basically requires only some implementation of
     * [AppCompatActivity] as host for UI. Nothing more. But you can add window styling here or
     * implement some interaction with system features like permissions etc.
     */

}
